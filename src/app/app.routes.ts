import { Routes } from '@angular/router';
import { PlanningCalendarComponent } from './components/planning-calendar/planning-calendar';

export const routes: Routes = [
    { path: '', component: PlanningCalendarComponent }
];
